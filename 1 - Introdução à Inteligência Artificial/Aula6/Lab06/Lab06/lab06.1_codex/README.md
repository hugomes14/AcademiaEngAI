# Lab 06.1 — Implementação para comparação

Esta pasta contém uma cópia de trabalho separada da pasta `lab06.1_sol`.
Foi criada para permitir a comparação entre implementações sem alterar a solução original.

Executar todas as etapas:

```bash
python lab_orquestrador.py --etapas 01 02 03 04 05 06 07
```

Os resultados são guardados em `outputs/`.
